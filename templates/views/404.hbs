<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="/css/styles.css">
    </head>
    <body>
        <div class="main-content">
            {{>header}}
            <h2>{{errorMessage}}</h2>
            {{>footer}} 
        </div>
        
    </body>
</html>